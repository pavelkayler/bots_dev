1) Located the per-run replay path in `frontend/src/pages/optimizer/OptimizerPage.tsx` inside the WebSocket `snapshot` handler. It rebuilt `rowsById` + `order` from scratch and called `setSingleResultsState({ rowsById, order, version: Date.now() })` whenever a snapshot arrived for the active job, which replaces row object identities and can make active-run rows replay/remount.

2) Confirmed current-run append path (`optimizer_rows_append` -> `queueLiveRowsAppend` -> `flushLiveRowsForActiveJob`) already used append/merge semantics with `Map` + `order`, so the replay issue was specifically from snapshot replacement during an active run.

3) Changed snapshot behavior for the active non-loop job:
   - If current store is empty, keep one-time replace for hydration.
   - Otherwise merge snapshot rows into existing `Map`/`order` without clearing/resetting order.
   - Append only new ids; update existing ids in place.
   - Added debug logs for `snapshot-replace`, `snapshot-merge`, and `snapshot-merge-noop` under `localStorage.debugRunAppendOnly === "1"`.

4) Added DEV-only reset instrumentation under `localStorage.debugRunAppendOnly === "1"`:
   - Logs when current-run store resets due to `clearSingleJobState`.
   - Logs when active job id changes and store is reset for the new job.

5) Added DEV-only row mount instrumentation for append-only proof:
   - `OptimizerResultRow` now logs mount/unmount for first two rows and the latest appended row id when `debugRunAppendOnly` is enabled.
   - A tracked appended row id ref is updated when a new row id is appended from either live append batches or snapshot merge.

6) Kept loop progress/status logic untouched and localized all changes to optimizer results rendering/state merge logic.

7) Verification performed:
   - `npm run build` in `frontend` passed.
   - Code path verification confirms no per-run `visibleCount`/slice/timer reveal exists in optimizer results rendering; active-run replay source was snapshot full replacement and is now merge-only once hydrated.
