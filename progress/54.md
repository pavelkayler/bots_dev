1. CI failed in the build-test job during setup-node because npm caching was enabled without a cache-dependency-path, so the action searched for a lockfile at the repository root and did not find one.
2. Updated .github/workflows/ci.yml to keep Node 22.x and npm caching enabled while adding cache-dependency-path entries for monorepo lockfiles at backend/package-lock.json and frontend/package-lock.json.
3. Simplified dependency install steps for backend and frontend to use npm ci directly, matching the existing lockfile-based setup.
4. Verification: push this branch and confirm the build-test workflow runs successfully with setup-node cache initialization passing and subsequent backend/frontend build steps green.
