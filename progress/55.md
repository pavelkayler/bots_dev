1. Removed remaining backend tape wiring from optimizer HTTP paths so optimizer execution and trade replay now resolve solely from `datasetHistoryIds` + `cacheDatasets` payload data.
2. Deleted legacy backend tape modules (`tapeRecorder`, `tapeStore`, `tapeRunsStore`) from `backend/src/optimizer`.
3. Kept config normalization backward-compatible for legacy tape keys (legacy keys are still stripped safely in config store).
4. Updated optimizer docs/contracts to state tape endpoints are removed and dataset histories/cache are the only optimizer data source.
5. Unified pagination UX further by migrating optimizer dataset-history table to the shared `TablePaginationControls` + persisted rows-per-page (`10/25/50`) via `useStoredPageSize`.
6. Local verification:
   - Backend build: `npm --prefix backend run build`
   - Frontend build: `npm --prefix frontend run build`
   - Tape search status: backend source no longer imports or exposes tape modules/routes; remaining legacy tape-key normalization is compatibility-only.
