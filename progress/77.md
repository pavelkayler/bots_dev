1. Context
- Continued the in-progress multi-bot isolation hardening with targeted cleanup:
  - Signal/OI typing separation on frontend `RuntimeConfig.botConfig`
  - Signal-bot label semantics in optimizer tables and legends
  - removed bright history-details background that conflicted with the dark UI

2. What was changed
- Updated `frontend/src/shared/types/domain.ts`:
  - widened `RuntimeConfig.botConfig` to support both bot schemas:
    - OI Momentum shape
    - Signal Multi-Factor shape
  - this removes the stale OI-only type assumption on shared frontend config paths.

- Updated `frontend/src/pages/optimizer/OptimizerPage.tsx`:
  - introduced bot-aware range labels (`getRangeLabel`) with separate dictionaries for:
    - OI optimizer terms
    - Signal optimizer terms
  - wired headers in the main results table and history-details table to use bot-aware labels.
  - made results legend text bot-aware:
    - Signal bot now shows signal-oriented parameter naming instead of OI shorthand text.
  - changed expanded history details container style from light background (`#f5f5f5`) to theme-aware dark-friendly colors.

3. Verification
- Backend tests: `npm --prefix backend test` ✅
- Frontend tests: `npm --prefix frontend test` ✅
- Backend build: `npm --prefix backend run build` ✅
- Frontend build: `npm --prefix frontend run build` ✅

4. Notes
- Legacy preset APIs (`frontend/src/features/presets/*`, `backend/src/presets/presetStore.ts`, and related endpoints) are still present in repo for compatibility, but active bot pages use bot-scoped preset APIs.
- This step focused on non-breaking semantic/typing cleanup and did not change runtime execution semantics.
