# 71 - Stage 12 verification pass (multi-bot-ready split)

Date: 2026-03-06

## 1) Verification scope
Performed a focused pass for the architecture split targets:
- `selectedBotId` as first-class selection for runtime and optimizer flows
- bot registry boundary for current bot
- bot presets vs execution profiles split
- TP/SL remaining in bot config (not in shared execution profile)

## 2) Repository state confirmed

Backend:
- Bot registry exists (`backend/src/bots/registry.ts`) with current bot as registered entry.
- Runtime/optimizer start endpoints accept and apply bot selections.
- Config split persists:
  - bot config in bot presets
  - shared execution/session/risk in execution profiles
  - runtime compatibility projection remains available.

Frontend:
- Dedicated bot page exists (`/bots`) for bot settings/presets.
- Dashboard remains operator-focused.
- Optimizer page is bot-aware and passes bot selections in run payloads.

## 3) Additional hardening added
File:
- `backend/src/tests/configNormalization.test.ts`

Added test:
- unknown `selectedBotId` resolves to default registered bot (`oi-momentum-v1`) through registry fallback path.

This keeps selected bot resolution deterministic and backward-compatible for stale/invalid saved selections.

## 4) Stage 12 stability checks
Executed:
1. `npm --prefix backend test`
2. `npm --prefix frontend test`
3. `npm --prefix backend run build`
4. `npm --prefix frontend run build`

Result:
- all checks passed
- architecture split remains stable with current single-bot implementation

