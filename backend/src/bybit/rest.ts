/**
 * REST bootstrap for instruments-info (stub).
 * TODO: call /v5/market/instruments-info?category=linear and build symbol meta map.
 */
export async function fetchInstrumentsInfo(): Promise<unknown> {
  return null;
}
