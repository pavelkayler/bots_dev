/**
 * Session lifecycle orchestration (stub).
 * TODO: start/stop session, wire WS -> MarketStateStore -> StrategyEngine -> PaperBroker -> EventLogger -> wsHub
 */
export class SessionManager {
  start(): void {
    // TODO
  }

  stop(): void {
    // TODO
  }
}
